import { OnlynumaricinputDirective } from './onlynumaricinput.directive';

describe('OnlynumaricinputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlynumaricinputDirective();
    expect(directive).toBeTruthy();
  });
});
